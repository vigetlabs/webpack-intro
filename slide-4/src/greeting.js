// This is an ES6 module. The export keyword tells the module
// system to declare the "greet" namespace and point it to
// this function:
export function greet (name) {
  return "Hello, " + name + "!"
}
